{
  "name": "chestonlee-website",
  "version": "1.0.0",
  "description": "Personal static website",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build src/index.html",
    "prod": "rm -rf ./dist/ && parcel build src/index.html",
    "dev": "parcel src/index.html",
    "watch": "parcel watch src/index.html",
    "clean": "rm -rf ./dist/"
  },
  "author": "Cheston Lee (chestonlloyd@gmail.com)",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "cssnano": "^4.1.10",
    "parcel-bundler": "^1.12.4"
  }
}
